@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg border-0 p-4 text-center" style="max-width: 450px;">
        <div class="card-body">
            <h1 class="text-danger">
                <i class="fa-solid fa-circle-exclamation"></i> Acceso Denegado
            </h1>
            <p class="mt-3 text-muted">
                Lo sentimos, pero no tienes permiso para acceder a esta página.
                Por favor, inicia sesión con una cuenta válida.
            </p>
            <a asp-controller="Managed" asp-action="Login" class="btn btn-danger w-100 mt-3">
                <i class="fa-solid fa-user-lock"></i> Iniciar Sesión
            </a>
            <hr class="my-4">
            <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary w-100">
                <i class="fa-solid fa-house"></i> Volver al Inicio
            </a>
        </div>
    </div>
</div>
